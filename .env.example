# Flask settings
FLASK_APP=main.py
FLASK_ENV=production
FLASK_DEBUG=0

# Security
# IMPORTANT: Change this to a secure random key in production
SECRET_KEY=your-secret-key-here-change-in-production

# Database 
# Using SQLite - no DATABASE_URL needed (uses default SQLite configuration)
# DATABASE_URL is optional - if not set, will use SQLite by default

# Google Gemini API
# Required for AI features - get from Google AI Studio
GEMINI_API_KEY=your_gemini_api_key_here
# Optional: override model (default gemini-1.5-flash)
# GEMINI_MODEL=gemini-1.5-pro

# Render specific settings
PORT=10000
